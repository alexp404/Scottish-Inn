# Hotel Management Frontend

This is a lightweight React + Vite frontend scaffold for the Hotel Management application.

Getting started:

1. cd frontend
2. npm install
3. npm run dev

The project includes a small set of reusable components under `src/components` and global styles.
